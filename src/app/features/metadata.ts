import { pageMetadata } from "@/lib/page-metadata";

export const metadata = pageMetadata(
  "Platform",
  "Evermount financial infrastructure for market data, quantitative research, intelligence, risk and execution.",
  "/platform",
);
